# Test Environment Variables
# Used only during Jest test execution

NODE_ENV=test
JWT_SECRET=test-jwt-secret-key-for-automated-testing-only
JWT_EXPIRES_IN=1h
CLIENT_URL=http://localhost:5173

# Tests use MongoDB Memory Server - no real database needed
# MONGODB_URI is intentionally empty for tests

# Email service disabled in tests
EMAIL_SERVICE=test
EMAIL_USER=test@example.com
EMAIL_PASS=test-password

# Cloudinary disabled in tests
CLOUDINARY_CLOUD_NAME=test
CLOUDINARY_API_KEY=test
CLOUDINARY_API_SECRET=test

# External APIs disabled in tests
PEXELS_API_KEY=test-key